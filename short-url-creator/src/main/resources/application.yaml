spring:
  application:
    name: url-shortener
  data:
    redis:
      host: localhost
      port: 6379
      password: password
      timeout: 2s
  threads:
    virtual:
      enabled: true
  config:
      import: "optional:configserver:http://localhost:8888"
app:
  instance:
    id: ${INSTANCE_ID:0}
